
div
    //- Logo
    a.logo(href="#content-1" data-mf-scroll-to='content-1')
        img(src="/img/mf-logo-medium.svg"  onerror="this.onerror=null; this.src='/img/mf-logo-medium.png'" alt="MindFlow logo")
    //- Nav links that should work same way with and without js turned on
    nav
        each article, index in getCollection(key).toJSON()
            //-a(ng-click="scroll('content-#{index+1}')") #{article.title}
            //-a(href="#content-#{index+1}" data-ng-click="scroll('content-#{index+1}')") #{article.title}
            a(href="#content-#{index+1}" data-mf-scroll-to='content-#{index+1}') #{article.title}
